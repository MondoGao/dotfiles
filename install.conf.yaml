- defaults:
    link:
      relink: true

- clean:
    - '~'

- link:
    ~/.dotfiles: ''
    ~/.sh: sh
    ~/.zshrc: zsh/.zshrc
    ~/.gitconfig: git/.gitconfig
    ~/.config/nvim: ~
    ~/.tern-config: .tern-config
